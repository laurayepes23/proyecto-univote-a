# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin
POSTGRES_DB=Univote

# Prisma / Backend
DATABASE_URL=postgresql://postgres:admin@db:5432/Univote?schema=public

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

#local
DATABASE_URL=postgresql://postgres:@localhost:5432/Univote?schema=public

# JWT Configuration
JWT_SECRET=CAMBIAR_ESTE_SECRETO_EN_PRODUCCION_MINIMO_32_CARACTERES_SEGUROS
JWT_EXPIRATION_ADMIN=8h
JWT_EXPIRATION_VOTER=24h
JWT_EXPIRATION_CANDIDATE=12h

# Server
PORT=3000
NODE_ENV=development

# Frontend
FRONTEND_URL=http://localhost:5173